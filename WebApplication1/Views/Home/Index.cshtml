<div class="imagen">
    <!-- formulario -->
    <div class="container-xxl  ">
        <div class="formulario1   ">
            <div class="formulario justify-content-center row ">
                <div class="mb-3 row">
                    <label for="formGroupExampleInput" class="form-label text-center">Numero de Cuenta</label>
                    <input type="text" onkeypress="return check(event)" onpaste="return false" class="form-control text-center" id="formGroupExampleInput" placeholder="123456789" maxlength="9">
                </div>
                <div class="col-12 text-center">
                    <button type="submit" onclick="Mostrar()" class="btn btn-primary">Consultar</button>
                </div>

            </div>
        </div>
    </div>
    <!-- formulario Contenor Fin -->
</div>

<script type="text/javascript">


    function check(e) {
        tecla = (document.all) ? e.keyCode : e.which;

        //Tecla de retroceso para borrar, siempre la permite
        if (tecla == 8) {
            return true;
        }

        // Patrón de entrada, en este caso solo acepta numeros y letras
        patron = /[0-9]/;
        tecla_final = String.fromCharCode(tecla);
        return patron.test(tecla_final);
    }
</script>











@*Enviar un mensaje a Michael Alexander Sevilla Martinez*@



